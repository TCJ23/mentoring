package gft.mentoring.matching;

public class Util {
    private Mentee mnt;
    private Mentor mtr;}

/*
    public boolean checkIfDev(Object o) {
        switch (o instanceof ) {
            case DIGITAL:
                return mtr.isDev = true;
            case DEVELOPMENT:
                return isDev = true;
            case ARCHITECTURE:
                return isDev = true;
            case DATA:
                return isDev = true;
            default:
                return false;
        }
    }
}
*/
